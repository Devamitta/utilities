# bash anki.sh

echo "~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~"
date

cd "/home/deva/Documents/dps/scripts"
python3 "suttaFilter.py"

echo "~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~"


while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done

while true; do
    read -p "Again?" yn
    case $yn in
        [Yy]* ) python3 "suttaFilter.py"; break;;
        [Nn]* ) exit;;
        *  ) echo "only yes or no";;
    esac
done
